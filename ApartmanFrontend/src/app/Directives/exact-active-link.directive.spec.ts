import { ExactActiveLinkDirective } from './exact-active-link.directive';

describe('ExactActiveLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ExactActiveLinkDirective();
    expect(directive).toBeTruthy();
  });
});
